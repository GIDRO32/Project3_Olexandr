package animals;

import soundmakers.SoundMaker;

public abstract class Insect implements SoundMaker {
    // Insect class implements SoundMaker interface but doesn't provide implementation for makeSound()
}
