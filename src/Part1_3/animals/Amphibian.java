package animals;

import soundmakers.SoundMaker;

public abstract class Amphibian implements SoundMaker {
    // Amphibian class implements SoundMaker interface but doesn't provide implementation for makeSound()
}

